import { Box } from "@mui/material";
import { styled } from "@mui/system";

export const StyledContainer = styled(Box)({ textAlign: "right" });

export const StyledButtons = styled(Box)({
  display: "flex",
  justifyContent: "end",
});

export const StyledButton = styled(Box)({ cursor: "pointer" });

export const StyledPage = styled(Box)({});

export const StyledContent = styled(Box)({
  fontStyle: "italic",
  margin: "0",
});
